import 'package:flutter/material.dart';

import '../conestants.dart';

class Favorites extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('المرااجعة',style: TextStyle(fontSize: 33,color: KTextColor)),
    );
  }
}
